<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>
        <record id="view_invoice_header_check_payment_button" model="ir.ui.view">
            <field name="name">account.move.header.check.payment.button</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="arch" type="xml">
                <xpath expr="//header/button[@name='action_register_payment']" position="after">
                    <button name="action_pay_with_check"
                            string="Çek ile Ödeme"
                            type="object"
                            class="btn-primary"
                            invisible="not (state == 'posted' and move_type in ['out_invoice', 'out_refund'])"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>
